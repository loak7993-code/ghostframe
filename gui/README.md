# GhostFrame GUI

Run from the project root: `npm run gui` (which runs `electron gui/`).
Manage profiles, launch browsers, and verify fingerprint readbacks visually.
Requires the core CLI at `src/cli/index.ts` (Stage 3) for launch / fingerprint / delete actions.
