{
  "name": "ghostframe-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "GhostFrame for Android — profile manager + live fingerprint verifier (Capacitor)",
  "scripts": {
    "sync": "cap sync android",
    "android": "cap open android",
    "build": "cap sync android && cd android && ./gradlew assembleDebug",
    "icons": "node tools/gen-icons.mjs"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.0",
    "@capacitor/android": "^7.4.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.0"
  }
}
